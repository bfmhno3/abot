# TODO

## 当前状态

- [ ] 确认当前仓库中的 `abot_find` 与上游 `find_object_2d` 是否保持同步，避免后续排障时把上游问题误判为本仓库改动。
- [ ] 复核编译选项、第三方依赖和工具程序（如 tcp 工具）是否仍符合当前 ROS / OpenCV 环境。
- [ ] 评估是否需要把本仓库自有改动与上游源码差异单独列出来，降低后续维护成本。

## 说明

这一轮排查主要聚焦任务层与驱动层，`abot_find` 暂未发现本仓库特有、已被直接证实的高优先级 bug，先保留为继续审查项。
