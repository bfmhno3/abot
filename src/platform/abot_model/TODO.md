# TODO

## 当前状态

- [ ] 继续审查 `abot_model` 的 URDF、mesh、Gazebo world 与 launch 配置，确认是否存在路径失效、模型资源缺失或仿真配置过时的问题。
- [ ] 复核导出的模型资源是否包含仅适用于原始开发环境的历史残留文件。

## 说明

当前这一轮排查里，`abot_model` 暂未发现像任务层那样已经被源码直接证实的高优先级逻辑 bug；这里先保留为待继续审查清单。
